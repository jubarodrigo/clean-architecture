# Alinhado ao 20-CleanArch (gqlgen).
schema:
  - internal/infra/graph/*.graphqls

exec:
  filename: internal/infra/graph/generated.go
  package: graph

model:
  filename: internal/infra/graph/model/models_gen.go
  package: model

resolver:
  layout: follow-schema
  dir: internal/infra/graph
  package: graph
  type: Resolver
